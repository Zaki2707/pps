import { FormEvent, useEffect, useState } from 'react';
import { api } from '../api';
type Book={id:string;title:string;author_text?:string;isbn?:string;category?:string;shelf_code?:string;total:number;available:number};
export function Books(){const [books,setBooks]=useState<Book[]>([]);const [form,setForm]=useState({title:'',isbn:'',author:'',publisher:'',year:'',category:'',shelf:'',quantity:'1'});const [msg,setMsg]=useState('');const load=()=>api<Book[]>('/api/admin/books').then(setBooks);useEffect(()=>{load()},[]);const submit=async(e:FormEvent)=>{e.preventDefault();try{const r=await api<any>('/api/admin/books',{method:'POST',body:JSON.stringify({...form,quantity:Number(form.quantity),year:form.year?Number(form.year):null})});setMsg(`✓ Buku dibuat dengan ${r.codes.length} eksemplar.`);setForm({title:'',isbn:'',author:'',publisher:'',year:'',category:'',shelf:'',quantity:'1'});load()}catch(e:any){setMsg(e.message)}};
return <><div className="page-head"><h1>Buku & Eksemplar</h1><p>Satu katalog dapat memiliki banyak eksemplar dengan barcode berbeda.</p></div><form className="book-form" onSubmit={submit}>{Object.entries(form).map(([k,v])=><label key={k}>{({title:'Judul',isbn:'ISBN',author:'Penulis',publisher:'Penerbit',year:'Tahun',category:'Kategori',shelf:'Rak',quantity:'Jumlah Eksemplar'} as any)[k]}<input value={v} onChange={e=>setForm(f=>({...f,[k]:e.target.value}))}/></label>)}<button className="primary">Simpan & Buat Eksemplar</button></form>{msg&&<div className="info-box">{msg}</div>}<div className="table-wrap"><table><thead><tr><th>Judul</th><th>Penulis</th><th>ISBN</th><th>Rak</th><th>Total</th><th>Tersedia</th></tr></thead><tbody>{books.map(b=><tr key={b.id}><td>{b.title}</td><td>{b.author_text}</td><td>{b.isbn}</td><td>{b.shelf_code}</td><td>{b.total}</td><td>{b.available}</td></tr>)}</tbody></table></div></>}
